# Dependencias de PRODUCCIÓN — esto es lo que construye Azure/Railway/Render.
# Solo lo que app/main.py importa en tiempo de ejecución (detect, evaluation,
# metrics). Los modelos comparativos y el tooling de análisis viven en
# requirements-dev.txt para que la imagen de despliegue sea liviana y rápida.

# ── API Framework ─────────────────────────────────────────────
fastapi
uvicorn[standard]
gunicorn
python-multipart
python-dotenv

# ── Imagen / Visión ───────────────────────────────────────────
pillow
numpy

# opencv-python-headless fijado a propósito: ultralytics depende de
# cv2 internamente y, si no se fija, pip instala opencv-python (con
# soporte de GUI) que requiere libGL.so.1 — ausente en el contenedor
# de Azure App Service y provoca ImportError al arrancar el worker.
opencv-python-headless

# ── PyTorch + YOLO26s (Ultralytics) ───────────────────────────
torch
torchvision
ultralytics

# ── LLM (descripción egocéntrica y clasificación de escenario) ─
groq

# ── Traducción dinámica EN→ES ─────────────────────────────────
deep-translator==1.11.4

# ── Text-to-Speech ──────────────────────────────────────────────
# edge-tts: voces neuronales de Microsoft Edge, gratis, sin API key ni
# tarjeta (a diferencia de Google Cloud TTS, que exige facturación
# activada en el proyecto incluso dentro de la capa gratuita).
edge-tts

# ── Cliente HTTP (usado por /api/test/* para pruebas de carga) ─
httpx
