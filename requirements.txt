# ── API Framework ─────────────────────────────────────────────
fastapi
uvicorn
python-multipart
python-dotenv

# ── Imagen / Visión ───────────────────────────────────────────
pillow
numpy
opencv-python

# ── PyTorch (YOLO26s y modelos comparativos) ─────────────────
torch
torchvision

# ── YOLO26s (Ultralytics) ──────────────────────────────────────
ultralytics

# ── Faster R-CNN y Mask R-CNN (experimental) ─────────────────
# Instalación especial - no disponible en PyPI estándar:
# pip install 'git+https://github.com/facebookresearch/detectron2.git'

# ── SSD-MobileNet V2 (experimental) ──────────────────────────
tensorflow
tensorflow-hub

# ── LLM (descripción egocéntrica y clasificación de escenario) ─
groq

# ── Traducción dinámica EN→ES ─────────────────────────────────
deep-translator==1.11.4

# ── Batch / Análisis / Evaluación ────────────────────────────
requests
pandas
matplotlib
psutil
openpyxl

# ── Text-to-Speech API (Google Cloud) ─────────────────────────
google-cloud-texttospeech

# ── Pruebas de carga y funcionales (nuevos endpoints) ─────────
httpx